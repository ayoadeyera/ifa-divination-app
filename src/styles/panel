/* --- Layer 4: The Scroll Panel --- */
.scroll-panel {
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 70vh; /* Takes up bottom 70% */
    z-index: 10;
    transform: translateY(110%); /* Hidden (pushed down) by default */
    transition: transform 0.8s cubic-bezier(0.2, 0.8, 0.2, 1);
}

/* The Paper Texture */
.parchment-texture {
    width: 100%;
    height: 100%;
    background-color: #e6dcc3; /* Old paper base color */
    /* Add a subtle noise texture if you have one, or keep flat color */
    border-top-left-radius: 30px;
    border-top-right-radius: 30px;
    padding: 40px 25px;
    box-sizing: border-box;
    box-shadow: 0 -10px 50px rgba(0,0,0,0.8); /* Shadow casting UP onto the mat */
    overflow-y: auto; /* Scrollable content */
}

/* Typography for the Reading */
.odu-title {
    font-family: 'Cinzel', serif;
    font-size: 2.5rem;
    color: #5a2d0c; /* Rust/Burnt Sienna */
    text-align: center;
    margin-bottom: 10px;
    text-transform: uppercase;
    letter-spacing: 2px;
}

.odu-divider {
    border: 0;
    height: 2px;
    background: linear-gradient(to right, transparent, #5a2d0c, transparent);
    margin: 20px auto;
    width: 60%;
}

.verse-text {
    font-family: 'Crimson Text', serif;
    font-size: 1.3rem;
    line-height: 1.6;
    color: #1a1510;
    text-align: center;
    max-width: 600px;
    margin: 0 auto;
}

.verse-yoruba {
    font-style: italic;
    font-weight: 600;
    margin-bottom: 20px;
    color: #2c241b;
}

.verse-english {
    color: #4a4a4a;
}

/* The Active State (Triggered by JS) */
.state-reading .scroll-panel {
    transform: translateY(0); /* Slide Up */
}